{
  "extends": "../../tsconfig.json", // Assuming a root tsconfig will be created later
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
