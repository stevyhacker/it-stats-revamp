{
  "name": "scripts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "bun run setup-db.ts",
    "import": "bun run import-data.ts",
    "import:irms": "bun run import-irms-processed.ts",
    "full-import": "bun run setup && bun run import"
  },
  "dependencies": {
    "@types/pg": "^8.20.0",
    "pg": "^8.21.0"
  }
}
